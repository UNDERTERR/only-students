<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onlystudents.note.mapper.NoteTagMapper">

    <!-- 批量查询标签 -->
    <select id="selectByNames" resultType="com.onlystudents.note.entity.NoteTag">
        SELECT * FROM note_tag 
        WHERE name IN 
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
        AND status = 1
    </select>

    <!-- 增加标签使用次数 -->
    <update id="incrementUsageCount">
        UPDATE note_tag 
        SET usage_count = usage_count + 1,
            updated_at = NOW()
        WHERE id = #{tagId}
    </update>

    <!-- 减少标签使用次数 -->
    <update id="decrementUsageCount">
        UPDATE note_tag 
        SET usage_count = CASE 
            WHEN usage_count > 0 THEN usage_count - 1 
            ELSE 0 
        END,
        updated_at = NOW()
        WHERE id = #{tagId}
    </update>

</mapper>
