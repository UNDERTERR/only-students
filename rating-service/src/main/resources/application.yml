server:
  port: 8007

spring:
  application:
    name: rating-service
  profiles:
    active: dev   # 以后切环境只改这里
  config:
    import:
      - optional:nacos:rating-service-${spring.profiles.active}.yaml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: ""
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: ""
        group: DEFAULT_GROUP
        import-check:
          enabled: false